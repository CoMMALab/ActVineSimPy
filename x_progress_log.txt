    My Notes on how things work:
        - How rectangles are described: [top_left_x, top_left_y, bottom_right_x, bottom_right_y]
    
    My ToDos:
        - add some way to acknowledge certain boxes as dynamic
                - change how obstacles are read in (new group: dynamic obstacles)

                - #FIXME: how do you handle environments where there's no dynamic obstacles?
                            (NOTE: this never happens for static ones because the walls are static)
                        - might have already fixed this, but double check to make sure you know exactly what's going on

                - #FIXME: why does test env crash when trying to look for cached solutions, even when --noplan is not being used???
                        - no cache directory exists (FIXED, by adding in directory)

                - #NOTE: did not check everywhere load_box_config is used (hopefully it will be handled automatically by planner stuff)

        - figure out out how to give dynamic boxes their own bounding boxes (should be same as static, right?)
                - #TODO: now make it that dyanamic objects, being registered as dyanmic, are affected like static objects (for now; will change their behavior with physics part)
        
        - implement physics (hard part)

    Other Stuff:
        