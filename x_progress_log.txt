        My ToDos:
        - implement physics (hard part)

                - collision penalty: 
                        - keep track of all dynamic object positions in cspace (extend the cspace)
                        - use updated coords (from gradient descent) to recalculate collision penalty 

                        - ALSO need to incorporate new term into penalty (instead of vine collides with objects, also dynamic objects collides with any other object)

                - inertial penalty:
                        - use change in dynamic object as parameter in equation to be used during gradient descent
                                - how much can we move the dynamic object in order to minimize the term?
                        - remember to update the cspace afterwards with the new dynamic object's position (essential for inertial and collision penalties)

                        - for distance that it moves:
                                - store past distance, then use current distance to find it (since this is done using gradient descent, just adjusting
                                weights until a solution that makes sense)

                - for changes in sst: just work on modifying rollout (what actually calls forward for the sim):
                        - make batched forward step return new dynamic body locations so that JAX stops bugging out
                        - then take these new positions and update the environment:
                                - re-render everything
                                - make sure the next sim_params object that is used is updated as well


                - BUG: everything is nan for dynamic objects:
                        - inertial grad in pdb_solve is nan
                        - look back at the calculation and check how differentiation is being done
                          (especially with how that function gets its inputs)